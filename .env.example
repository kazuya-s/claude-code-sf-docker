# ホストユーザーの UID/GID — ホストとコンテナ間のファイル所有権を合わせるために使用します。
#
# macOS:
#   UID=$(id -u)  → 通常 501
#   GID=$(id -g)  → 通常 20 (staff)
#
# Linux:
#   UID=$(id -u)  → 通常 1000
#   GID=$(id -g)  → 通常 1000
#
# ホストで `id -u` および `id -g` を実行して正しい値を確認してください。

UID=1000
GID=1000
